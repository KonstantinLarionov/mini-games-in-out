import { Routes } from '@angular/router';
import {BalancePageComponent} from '../balance.page.component/balance.page.component';

export const routes: Routes = [
  { path: '', component: BalancePageComponent }
];
